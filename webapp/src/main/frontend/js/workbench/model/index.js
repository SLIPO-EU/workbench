import * as RoleNames from './role';

export {
  EnumKpiViewMode,
  EnumProcessSaveAction,
} from './constants';

export {
  buildPath,
  DynamicRoutes,
  StaticRoutes,
} from './routes';

export const Roles = {
  ...RoleNames,
};
