import {
  EnumDataFormat,
} from "../enum";

export const defaultValues = {
  inputFormat: EnumDataFormat.N_TRIPLES,
  outputFormat: EnumDataFormat.N_TRIPLES,
  outputDir: "output",
  spec: null,
  profile: null,
};
